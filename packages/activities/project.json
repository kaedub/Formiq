{
  "name": "activities",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/activities/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn tsc -b packages/activities/tsconfig.json"
      },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
